(include_subdirs unqualified)

(library
 (name pl0c_test)
 (libraries core ast parse semant opt ir mir isel liveness)
 (preprocess
  (pps ppx_jane))
 (inline_tests
  (deps
   (source_tree ../examples))))
